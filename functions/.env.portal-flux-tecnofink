# Parâmetros das Functions em produção.
# E-mail (RF-51): sem SMTP configurado, os e-mails são apenas registrados no log.
# Para ativar, defina SMTP_HOST/SMTP_USER e o segredo SMTP_PASS (ver README).
SMTP_HOST=smtp.gmail.com
SMTP_USER=ti@tecnofink.com
EMAIL_FROM=Portal Flux <ti@tecnofink.com>
