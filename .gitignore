node_modules/
dist/
*.local
functions/lib/
.tools/
firebase-debug.log
firestore-debug.log
ui-debug.log
*.log
.firebase/

# arquivos de lock do Office
~$*
scratchpad/

# segredos — NUNCA versionar (repo público). Os functions/.env.* de parâmetros
# (sem senha; a senha vive no Secret Manager) são commitados de propósito.
Dados Supabase*.txt
*serviceaccount*.json
*service-account*.json
*-key.json
credentials.json
.env
.env.production.local
